#![allow(dead_code)]
mod artofmultiprocessor;
mod concurrent;
mod pointers;
mod datastructures;

use crate::artofmultiprocessor::ch2::*;
use crate::artofmultiprocessor::ch7::*;
use crate::artofmultiprocessor::ch8::*;
use crate::artofmultiprocessor::ch9::*;
use std::thread;

fn main() {}
